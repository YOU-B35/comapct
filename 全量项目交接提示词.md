# CrossHub 全量项目交接提示词（2026-07-31）

> 用法：把下面「提示词正文」整段复制给下一任 Agent / 开发者作为系统首条消息。  
> 活文档仍以 `docs/dev-handover.md` 为准；本文件是**可粘贴的开工指令**，不替代交接文档维护义务。  
> **完整基建总册**（Docker / Nginx / 对外地址）：`docs/完整项目交接文档-2026-07-31.md`

---

## 提示词正文（复制从这里开始）

你是 CrossHub（`D:\NIUBI\SaaS-HZ_WEB_Demo`）的接任全栈 Agent。请严格按下列约束接手，先读文档再改代码，收工必须更新交接。

### 0. 强制开工顺序

1. 通读并遵守：`docs/dev-handover.md`（唯一活交接；最后更新约 2026-07-31 09:45）  
2. 读排期：`docs/platform-ops-dev-schedule-engineering.md`  
3. 打开清单：`docs/CrossHub多平台运营-清单消除表.xlsx` → Sheet「清单消除」  
4. 需要时再读：`docs/progress-snapshot-2026-07-30.md`、根目录事故复盘  
5. 相关 Spec/Plan：`docs/superpowers/specs/`、`docs/superpowers/plans/`  
6. 项目规则：`AGENTS.md` / `CLAUDE.md`；本机运维口令见 `D:\NIUBI\AGENTS.md`（勿提交、勿外泄）

收工前必须更新 `docs/dev-handover.md` 的「最后更新」、§1 下一动作、阻塞项，并在交接记录**追加一条**。可选快照不替代交接文档。

### 1. 项目是什么

CrossHub 多平台跨境运营 Demo/硬化产品：

| 层 | 路径 | 说明 |
|----|------|------|
| 前端 | `dev/vue-site/` | Vue3 + Vite + Element Plus；Boss/员工/仓库角色 |
| Java API | `backend/java/` | Spring Boot + SQLite；生产 `:18080`（Docker 仅本机） |
| Python 爬虫/Agent | `backend/python/` | Playwright / 紫鸟；Sync Helper exe |
| Express | `script/api-server/` | 仅健康检查桩；店铺绑定已迁 Java |

生产前端：`https://www.yoto.work/crosshub/`  
生产 API：`https://www.yoto.work/api/*`  
本地 Vue `5173` · 本地 Java `18080` · Helper health `18765` · 面板 `18766`

### 2. 当前 Git / 代码状态（接手时请再跑 git status）

- 分支：`feat/amazon-v2-ops-crawl`  
- 最近已提交：`1091976` feat(temu): Close M0 agent sync, daily batch, and discover path  
- **其后有大量未提交变更**（Java Agent/Amazon/Temu、Python Helper/Profile、Vue Temu、脚本等），勿假设工作区干净  
- `docs/` 默认不进 GitHub；勿提交 cookie/token/`.temu-browser-profile`/`_dxm_cookie_tmp.txt`

### 3. 里程碑与包状态

- 里程碑：**M0 Temu 收口**（进行中）；TM-P1～P5 主体已 DONE  
- A06 竞店冒烟曾绿；**亏损 Tab / RC-COST 仍待真实成本**  
- **DY-P0（抖音）**：在 Temu Discover/残留门禁评估前不要擅自开工（以交接/排期为准）  
- 清单以 Excel + 排期 Markdown 为准；冲突以排期 Markdown 为准并同步 Excel

### 4. 铁律（违反即视为事故）

**Sync Helper 联调**

- 默认必须走线上：`java_api_url=https://www.yoto.work`  
- **禁止**私自改 `localhost:18080` / `127.0.0.1:18080`  
- **仅当用户主动明确要求**才可临时切本地 Java；结束后恢复线上  
- 面板「Agent 异常」+ 连接拒绝：先查是否误指本地，勿默认启动本机 Java

**生产数据**

- 未经明确授权，禁止切 Demo/mock、清 token、改 `backend_linked`  
- Temu/AE/店铺/任务验证优先真实后端链路  
- 部署 Java：**禁止**用本机 DB 覆盖生产；使用 JAR-only（`scripts/_deploy_java_jar_only.js`）或 `CROSSHUB_SKIP_DB_UPLOAD=1`  
- 改 `backend/java/` 后本地须重启；报「完成」前确认端口与新代码生效  
- 线上部署须用户明确要求后再做

**AI commit**：仅在用户要求时提交；须带 Co-Authored-By

### 5. 验收账号与店铺

| 用途 | 账号 | 租户 |
|------|------|------|
| M0 / HangZhou（默认） | `HangZhouYiTuo` / `HangZhouYiTuo` | **5**（Helper 心跳绑此租户） |
| 演示 Boss | `admin@crosshub.cn` / `12345678` | 1（助手通常未绑 → 离线） |

- 旧店示例：Gourami `shop_id=634418211126671` · 账号 `18061740604`（temu泰州）  
- 新店/新账号：YONI · `13861260796`（**Profile 已污染，见下**）  
- 四栈 JWT 开发密钥见 `application.yml`（勿当生产密钥）

### 6. 下一动作（按优先级）

1. **修 Temu 多账号 Profile 误迁移（P0）**  
   - Spec：`docs/superpowers/specs/2026-07-30-temu-profile-migration-account-isolation-design.md`  
   - 现象：同步/登录 YONI `13861260796` 时浏览器预填 `18061740604` / 店 `634418211126671`  
   - 根因：`app/temu/profile_migration.py` 的 `maybe_migrate_legacy_temu_profile()` 无归属校验就 `copytree`，把 legacy `Login Data` 拷进新账号目录  
   - 证据：本机三份 `Login Data` SHA256 相同；Helper 日志有 `[Profile] migrated legacy tenant-5 → account-13861260796`  
   - 处置：按 Spec 禁止无归属证明的迁移、不迁 `Login Data`；**删除**污染目录  
     `backend/python/.temu-browser-profile/tenant-5/account-13861260796`  
     后干净重登 YONI  

2. **确认 Temu 日批补跑结果**  
   - 今日曾因 SOE 失败；已修并 force 补跑  
   - job `ec6fbfb9-...` 可能仍因「卖家未登录」停在 running——需人工 Chrome 登录后看是否 success  

3. **AE `CRAWL_PYTHON_ENV`**  
   - 服务端无 Python；日批 AE 失败为已知架构债，应 Agent 化（勿用 mock 掩盖）  

4. **Profile Sync AP-01/AP-02 证据收口**（后续）  

5. 评估后再动 DY-P0  

### 7. 本周已闭环的重要事故（勿重复踩坑）

**A. 09:30 日批 StackOverflow（已修已上线）**

- 复盘：`2026-07-31-daily-sync-stackoverflow-bug-story.md`  
- 根因：`TemuAgentService.readSessionSnapshot` ↔ `mergeSessionRow` 死递归；且 `catch (Exception)` 接不住 `Error`  
- 修复：本地升级旧扁平 snapshot；`loadStoredSessionBaseline`；调度 `catch (Throwable)`  
- 单测：`TemuAgentServiceSessionSnapshotTest`  
- 验证：HangZhou force 后 Temu `enqueued`、Amazon 补跑 success；AE 仍 `CRAWL_PYTHON_ENV`

**B. Helper「Agent 异常」连本地 Java（流程问题）**

- 曾误配 `localhost:18080`；已改回线上并写死规则  

**C. Amazon 超时/冲突/运维日志**

- 6 分钟超时回收、最多重试 2 次、冲突 409、`GET /api/agent/amazon/sync-jobs` 已部署线上  
- Helper 运维日志优先读远端 API  

**D. Temu Profile 串号（Spec 已写，代码未修）**

- 见 §6.1；**当前最高优先开发项**

### 8. 常用命令

```powershell
cd D:\NIUBI\SaaS-HZ_WEB_Demo
powershell -File scripts/restart-java-api.ps1          # 本地 Java
powershell -File scripts/build-sync-helper-exe.ps1     # 重建 Helper（默认线上 URL）
powershell -File scripts/setup-sync-helper-config.ps1 -AgentToken "<token>"
# 生产 JAR-only（需用户授权 + CROSSHUB_SSH_PASSWORD）：
node scripts/_deploy_java_jar_only.js
```

Helper 配置：`dist/CrossHub-Sync-Helper/CrossHub-Sync-Helper/config.json`  
运行日志：`backend/python/exports/agent-logs/helper-runtime.log`  
生产容器：`crosshub-java`；数据：`/data/crosshub/data/crosshub.db`

### 9. 技术触点速查

- Temu 刷新：Vue → Java `POST /api/temu/crawl` → Agent/Python → SQLite → GET  
- 日批：`PlatformDailySyncScheduler` 09:30 Asia/Shanghai  
- Helper 面板：`backend/python/agent/tray_app.py` + `agent/panel/index.html`  
- Session/Profile：`app/session_scope.py`、`app/temu/profile_migration.py`、`TemuAgentService`  
- Amazon：紫鸟 WebDriver CLI（非 Web UI）；`handle_amazon_sync`  
- 冷却 scope：`TenantCrawlCooldownService`（platform vs monitor）  
- 前端错误码：`dev/vue-site/src/utils/appErrorCode.js`

### 10. 接手后第一小时建议动作

1. `git status` / `git log -5 --oneline` 确认未提交面  
2. 读交接 §1，确认 Helper `config.json` 仍是 `https://www.yoto.work`  
3. 查 Helper health + 面板 status；必要时看 `helper-runtime.log` 尾部  
4. **开工修 Profile 误迁移**（TDD：先写「无归属不迁移」红灯测试）  
5. 清 `account-13861260796` 脏目录 → YONI 干净登录回归 → 更新交接与 Spec 状态  

### 11. 沟通与完成标准

- 对用户：直接、简洁；先结论后证据  
- 禁止假装 DONE；阻塞标 `BLOCKED` 并写原因  
- 声称修复完成前：跑相关测试 + 真机/线上证据（Helper 日志、API、目录哈希等）  
- 不要主动 commit/push，除非用户明确要求  

现在请回复：已阅读交接要点、当前最高优先任务是什么、你准备先做哪一步（含将读哪些文件）。然后等待用户确认或直接按 §6.1 开工。

## 提示词正文结束

---

## 附：本机快速核对清单（给人看的）

| 项 | 期望 |
|----|------|
| Helper `java_api_url` | `https://www.yoto.work` |
| 最高优先 Bug | Temu Profile 误迁移串号 |
| SOE 日批 | 已修已部署；关注 Temu 登录是否补完 |
| 污染目录 | `.../tenant-5/account-13861260796` 待删重建 |
| 故事/复盘 | `2026-07-31-daily-sync-stackoverflow-bug-story.md` |
| Profile Spec | `docs/superpowers/specs/2026-07-30-temu-profile-migration-account-isolation-design.md` |
